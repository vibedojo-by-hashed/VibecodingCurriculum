# Chapter 05: 터미널 명령어

[English](./README.md) | **한국어**

---

## 💬 질문하기

학습 중 궁금한 점이 있으면 디스코드에서 질문하세요!

[![Discord](https://img.shields.io/badge/Discord-질문하기-5865F2?style=for-the-badge&logo=discord&logoColor=white)](https://discord.gg/TxbJ56hS94)

---

## 🎯 이 챕터의 목표

- Claude에게 명령어 실행시키는 법 배우기
- 자주 사용하는 터미널 명령어 알기
- npm 생태계 이해하기
- 서버 개념과 로컬 개발 환경 이해하기
- 프로젝트 실행 및 테스트하기

---

## ⏱️ 예상 소요 시간

- 읽기: **30분**
- 실습: **30분**

---

## 📋 필요한 것

- Claude Code 설치 및 로그인 완료
- 파일 다루기 기본 이해 (Chapter 04)

---

## 🔗 이전 챕터 복습

[Chapter 04](../Chapter04-Working-with-Files/README.ko.md)에서 우리는 `@`로 파일을 참조하고, 파일을 생성/수정/삭제하는 방법을 배웠습니다. 이제 파일에 "생명을 불어넣는" 터미널 명령어를 배울 차례입니다.

---

## 왜 필요합니까?

파일만으로는 소프트웨어가 작동하지 않습니다. 도구를 설치하고, 서버를 시작하고, 코드를 테스트하려면 명령어를 실행해야 합니다. 명령어는 파일에 생명을 불어넣는 행동이라고 생각하세요.

**실제 상황:**
- 웹사이트를 만들었고, 이제 브라우저에서 보고 싶다
- 새 기능을 추가하려고 패키지를 설치해야 한다
- 코드가 제대로 작동하는지 테스트하고 싶다
- 코드에 에러가 있어서 디버그해야 한다

### 쉬운 비유: 주방 가전

파일이 재료라면, 터미널 명령어는 주방 가전입니다.

```
┌────────────────────────────────────────────┐
│           주방 가전 비유                      │
├────────────────────────────────────────────┤
│                                            │
│  npm install  = 장보기 (재료 구하기)          │
│  npm run dev  = 불 켜기 (요리 시작)          │
│  npm test     = 맛보기 (잘 됐나 확인)         │
│  npm run build= 포장하기 (손님께 드릴 준비)    │
│                                            │
│  가스레인지가 어떻게 작동하는지 몰라도         │
│  "불을 켜면 요리할 수 있다"만 알면 됩니다.     │
│                                            │
└────────────────────────────────────────────┘
```

가스레인지가 내부적으로 어떻게 작동하는지 알 필요 없습니다. "불을 켜면 요리할 수 있다"만 알면 됩니다. 명령어도 마찬가지입니다. Claude가 세부 사항을 알고, 여러분은 원하는 것만 말하면 됩니다.

---

## 터미널의 역사와 중요성

### 왜 터미널이 아직도 중요한가요?

GUI(그래픽 인터페이스)가 있는데 왜 아직도 터미널을 사용할까요?

| 측면 | GUI (그래픽) | CLI (터미널) |
|------|-------------|--------------|
| **속도** | 마우스 이동, 클릭 | 타이핑으로 빠르게 |
| **자동화** | 어려움 | 스크립트로 쉽게 |
| **원격 작업** | 화면 공유 필요 | 텍스트만으로 가능 |
| **정밀성** | 제한적 | 세밀한 제어 가능 |
| **서버 관리** | 대부분 불가능 | 필수 |

### 개발자들이 터미널을 쓰는 이유

1. **효율성**: 같은 작업을 더 빠르게
2. **자동화**: 반복 작업을 스크립트로
3. **서버 접근**: 클라우드 서버에는 GUI가 없음
4. **도구 통합**: 대부분의 개발 도구가 CLI 지원
5. **표준**: 전 세계 개발자들이 같은 방식 사용

> 💡 **비전공자 팁**
>
> 터미널을 두려워할 필요 없습니다!
> Claude Code와 함께라면 명령어를 외울 필요 없이
> "서버 켜줘"라고 말하면 됩니다.

### 터미널 역사 간단히

```
1960년대: 텔레타이프 (물리적 타자기)
    ↓
1970년대: 터미널 화면 등장
    ↓
1980년대: 개인용 컴퓨터, DOS
    ↓
1990년대: Windows GUI, 하지만 개발자는 계속 터미널 사용
    ↓
2000년대~: 웹 서버, 클라우드 → 터미널 더 중요해짐
    ↓
현재: AI와 함께 자연어로 터미널 명령어 실행
```

---

## Claude에게 명령어 실행시키기

Claude Code는 터미널 명령어를 대신 실행할 수 있습니다.

### 자연어로 요청하기

```
> 이 폴더에 있는 파일 목록 보여줘
```

Claude가 `ls` 명령어를 실행해서 표시합니다.

```
> node 버전 확인해줘
```

Claude가 `node --version`을 실행합니다.

```
> 숨김 파일도 포함해서 다 보여줘
```

Claude가 `ls -la`를 실행합니다.

### 승인 절차

명령어를 실행하기 전에 Claude가 확인을 요청합니다:

```
Claude wants to run: ls -la
[Allow] [Deny]
```

`Allow`를 선택하면 실행됩니다.

> 💡 **비전공자 팁**
>
> 터미널 명령어를 외울 필요 없습니다!
> "파일 목록 보여줘"라고 하면 Claude가 알아서 `ls`를 실행합니다.
> 자연어로 말하면 됩니다.

### ! 접두사로 바로 실행

`!`를 붙이면 명령어를 바로 실행하고 결과를 Claude에게 전달합니다.

```
> !npm run build
```

빌드를 실행하고, 결과(성공/실패)를 Claude가 인식합니다.

```
> !git status
```

git 상태를 확인하고, Claude가 그 내용을 인식합니다.

---

## 필수 터미널 명령어 20가지

직접 외울 필요는 없습니다. 그러나 기본 명령어를 알아두면 더 정확하게 요청할 수 있습니다.

### 파일/폴더 관련 (7가지)

| 명령어 | 기능 | 자연어로 요청 | 예시 |
|--------|------|--------------|------|
| `ls` | 파일 목록 보기 | "파일 목록 보여줘" | `ls -la` |
| `cd` | 폴더 이동 | "src 폴더로 이동해줘" | `cd src` |
| `mkdir` | 폴더 생성 | "new-folder 폴더 만들어줘" | `mkdir new-folder` |
| `rm` | 파일 삭제 | "이 파일 삭제해줘" | `rm file.txt` |
| `cp` | 파일 복사 | "이 파일 복사해줘" | `cp a.txt b.txt` |
| `mv` | 파일 이동/이름 변경 | "이 파일 이름 바꿔줘" | `mv old.txt new.txt` |
| `pwd` | 현재 위치 확인 | "지금 어디야?" | `pwd` |

### 프로젝트 관련 (7가지)

| 명령어 | 기능 | 자연어로 요청 | 예시 |
|--------|------|--------------|------|
| `npm install` | 패키지 설치 | "패키지 설치해줘" | `npm install express` |
| `npm run dev` | 개발 서버 실행 | "서버 켜줘" | `npm run dev` |
| `npm run build` | 빌드 | "빌드해줘" | `npm run build` |
| `npm test` | 테스트 실행 | "테스트 돌려줘" | `npm test` |
| `npm init` | 프로젝트 초기화 | "npm 초기화해줘" | `npm init -y` |
| `npx` | 패키지 실행 | "create-react-app 실행해줘" | `npx create-react-app my-app` |
| `npm uninstall` | 패키지 삭제 | "lodash 삭제해줘" | `npm uninstall lodash` |

### Git 관련 (4가지)

| 명령어 | 기능 | 자연어로 요청 | 예시 |
|--------|------|--------------|------|
| `git status` | 상태 확인 | "git 상태 보여줘" | `git status` |
| `git add` | 스테이징 | "변경사항 추가해줘" | `git add .` |
| `git commit` | 커밋 | "커밋해줘" | `git commit -m "메시지"` |
| `git push` | 푸시 | "푸시해줘" | `git push origin main` |

### 기타 유용한 명령어 (2가지)

| 명령어 | 기능 | 자연어로 요청 | 예시 |
|--------|------|--------------|------|
| `cat` | 파일 내용 보기 | "파일 내용 보여줘" | `cat config.json` |
| `grep` | 텍스트 검색 | "error 찾아줘" | `grep -r "error" src/` |

> 🔥 **프로 팁**
>
> 명령어를 외우려 하지 마세요.
> Claude에게 "npm 자주 쓰는 명령어 알려줘"라고 하면 됩니다.
> 필요할 때마다 물어보면서 자연스럽게 익혀가세요.

### 명령어 옵션 이해하기

명령어 뒤에 붙는 `-`나 `--`는 옵션입니다.

```bash
ls -l        # 자세히 보기
ls -a        # 숨김 파일 포함
ls -la       # 둘 다
npm install --save-dev  # 개발 의존성으로 설치
```

> 💡 **비전공자 팁**
>
> 옵션이 헷갈리면 Claude에게 물어보세요.
> "npm install에서 --save-dev가 뭐야?"

---

## npm 생태계 이해하기

### npm이란?

npm (Node Package Manager)은 자바스크립트 패키지 관리자입니다.

```
┌────────────────────────────────────────────┐
│           npm 생태계                         │
├────────────────────────────────────────────┤
│                                            │
│  📦 npm = 레고 블록 상점                      │
│                                            │
│  - 누군가 만든 코드(패키지)를 가져다 쓸 수 있음  │
│  - 직접 처음부터 다 만들 필요 없음             │
│  - 전 세계 개발자가 200만 개 이상 공유 중       │
│                                            │
│  예시:                                      │
│  - express: 웹 서버 만들기                   │
│  - react: UI 만들기                         │
│  - lodash: 유틸리티 함수들                    │
│  - axios: HTTP 요청                         │
│                                            │
└────────────────────────────────────────────┘
```

### 쉬운 비유: 레고 블록

```
처음부터 다 만들기:
흙 → 벽돌 → 집 (오래 걸림)

npm 사용하기:
레고 블록 가져오기 → 조립 → 완성 (빠름)
```

### package.json 이해하기

모든 Node.js 프로젝트의 핵심 파일입니다.

```json
{
  "name": "my-project",          // 프로젝트 이름
  "version": "1.0.0",            // 버전
  "scripts": {                    // 실행 가능한 명령어들
    "dev": "vite",                // npm run dev
    "build": "vite build",        // npm run build
    "test": "vitest"              // npm test
  },
  "dependencies": {               // 실행에 필요한 패키지
    "react": "^18.2.0",
    "express": "^4.18.0"
  },
  "devDependencies": {            // 개발에만 필요한 패키지
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}
```

> 💡 **비전공자 팁**
>
> package.json은 "프로젝트 설명서"입니다.
> - 어떤 패키지가 필요한지
> - 어떤 명령어로 실행하는지
> 모두 여기에 적혀 있습니다.

### 자주 사용하는 npm 명령어

| 명령어 | 의미 | 언제 사용? |
|--------|------|-----------|
| `npm install` | 모든 의존성 설치 | 프로젝트 처음 받았을 때 |
| `npm install <패키지>` | 특정 패키지 설치 | 새 기능 추가할 때 |
| `npm run <스크립트>` | 스크립트 실행 | 서버 시작, 빌드 등 |
| `npm init -y` | 새 프로젝트 생성 | 처음 프로젝트 시작할 때 |
| `npm update` | 패키지 업데이트 | 최신 버전으로 업데이트할 때 |

### dependencies vs devDependencies

| 구분 | dependencies | devDependencies |
|------|--------------|-----------------|
| 의미 | 실행에 필요 | 개발에만 필요 |
| 예시 | react, express | typescript, eslint |
| 설치 | `npm install 패키지` | `npm install -D 패키지` |
| 배포 시 | 포함됨 | 제외됨 |

> 🔥 **프로 팁**
>
> "이 패키지 dependencies에 넣어야 해, devDependencies에 넣어야 해?"
> 헷갈리면 Claude에게 물어보세요!

---

## 서버 개념과 로컬 개발

### 서버란?

```
┌────────────────────────────────────────────┐
│           서버 개념                          │
├────────────────────────────────────────────┤
│                                            │
│  서버 = 요청을 받아서 응답하는 프로그램         │
│                                            │
│  [사용자] → 요청 → [서버] → 응답 → [사용자]   │
│                                            │
│  예시:                                      │
│  - 구글에 접속 → 구글 서버가 검색 페이지 전송   │
│  - 유튜브에서 영상 클릭 → 서버가 영상 전송      │
│                                            │
└────────────────────────────────────────────┘
```

### 로컬 서버 vs 실제 서버

| 구분 | 로컬 서버 | 실제(프로덕션) 서버 |
|------|----------|-------------------|
| 위치 | 내 컴퓨터 | 클라우드 (AWS, GCP 등) |
| 접속 | localhost:3000 | www.example.com |
| 용도 | 개발, 테스트 | 실제 서비스 |
| 보안 | 나만 접속 가능 | 전 세계 접속 가능 |

### localhost란?

```
localhost = 내 컴퓨터 자신

localhost:3000 = 내 컴퓨터의 3000번 포트
```

포트 번호는 "문 번호"라고 생각하면 됩니다.
- 3000번 문: 개발 서버
- 8080번 문: 다른 서버
- 80번 문: 일반 웹 (http)
- 443번 문: 보안 웹 (https)

> 💡 **비전공자 팁**
>
> `localhost:3000`에 접속하는 것은
> "내 컴퓨터의 3000번 문을 두드리는 것"입니다.
> 서버가 켜져 있으면 응답이 오고,
> 꺼져 있으면 연결할 수 없습니다.

### 개발 서버 실행하기

```bash
# 개발 서버 시작 (package.json의 scripts에 정의됨)
npm run dev

# 서버가 시작되면 보통 이렇게 출력됨:
#   ➜  Local:   http://localhost:3000/
#   ➜  Network: http://192.168.1.100:3000/
```

**서버 종료:**
- `Ctrl + C` 누르기
- 터미널을 닫으면 서버도 종료됨

---

## 🔨 따라해보세요

### 실습 1: 환경 확인하기

```
> node 버전이 뭐야?
```

**예상 결과:** Claude가 `node --version`을 실행하고 버전 표시

```
> npm 설치되어 있어?
```

**예상 결과:** Claude가 확인하고 알려줌

### 실습 2: 간단한 프로젝트 만들고 실행하기

```
> test-server 폴더 만들어줘
```

```
> 그 폴더에서 npm 초기화해줘
```

```
> express 설치해줘
```

```
> index.js 만들어줘. "Hello World"를 보여주는 간단한 웹서버로.
```

```
> 서버 실행해줘
```

**예상 결과:** 서버가 실행되고, 브라우저에서 `http://localhost:3000` 접속 가능

### 실습 3: 에러 처리하기

에러가 나면:
```
> 방금 에러 났어. 뭐가 잘못된 거야?
```

**예상 결과:** Claude가 에러 원인 분석하고 해결책 제시

### 실습 4: Git 기본 사용

```
> git 저장소 만들어줘
```

```
> 현재 변경사항 커밋해줘
```

**예상 결과:** git init, git add, git commit 순서로 실행됨

### 실습 5: 패키지 탐색

```
> 날짜 다루는 좋은 패키지 추천해줘
```

```
> dayjs 설치해줘
```

```
> dayjs 사용 예제 보여줘
```

---

## 🖼️ 예상 화면

### 명령어 실행 승인

```
Claude wants to run command:
npm install express

Allow this action? [y/n/a]
```

### 서버 실행 결과

```
> 서버 실행해줘

Running: node index.js

Server running at http://localhost:3000

서버가 시작됐습니다! 브라우저에서 http://localhost:3000을 열어보세요.
```

### package.json 생성

```
> npm 초기화해줘

Running: npm init -y

Wrote to /Users/user/test-server/package.json:

{
  "name": "test-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}

package.json이 생성되었습니다.
```

---

## 🎯 미니 퀴즈

### Q1. npm은 무엇의 약자인가요?
<details>
<summary>정답 보기</summary>

Node Package Manager.
자바스크립트 패키지를 관리하는 도구입니다.
</details>

### Q2. `npm install`과 `npm run dev`의 차이는?
<details>
<summary>정답 보기</summary>

- `npm install`: 패키지를 설치합니다
- `npm run dev`: package.json의 scripts에 정의된 dev 명령을 실행합니다
</details>

### Q3. localhost:3000은 무엇을 의미하나요?
<details>
<summary>정답 보기</summary>

내 컴퓨터(localhost)의 3000번 포트입니다.
개발 서버에 접속하는 주소입니다.
</details>

### Q4. Ctrl+C는 터미널에서 무엇을 하나요?
<details>
<summary>정답 보기</summary>

현재 실행 중인 프로세스(예: 서버)를 중지합니다.
</details>

### Q5. dependencies와 devDependencies의 차이는?
<details>
<summary>정답 보기</summary>

- dependencies: 실행에 필요한 패키지 (예: react, express)
- devDependencies: 개발에만 필요한 패키지 (예: eslint, typescript)
</details>

---

## 📝 실습 과제

### 난이도 1: 기초 (필수)

1. 터미널에서 `node --version` 확인
2. 새 폴더 만들고 `npm init -y` 실행
3. `package.json` 내용 확인

### 난이도 2: 중급

1. Express 설치하기
2. 간단한 웹서버 만들기
3. 브라우저에서 확인하기
4. 서버 종료하기

### 난이도 3: 도전

1. 여러 라우트가 있는 API 서버 만들기
2. 테스트 스크립트 추가하기
3. git으로 버전 관리하기

---

## 🏆 도전 과제

1. **패키지 탐색**: npm에서 유용한 패키지 5개 찾아서 설치해보기
2. **스크립트 작성**: package.json에 커스텀 스크립트 추가하기
3. **배포 준비**: `npm run build`로 프로덕션 빌드 만들어보기

---

## 미니 프로젝트: 나만의 API 서버 ⭐

Hello World 서버를 확장해서 간단한 API를 만들어봅니다.

### 🟢 쉬움: 기본 API

```
> GET /api/hello 하면 { "message": "안녕하세요!" } 응답하는 API 추가해줘
```

### 🟡 보통: 여러 API

```
> GET /api/time 하면 현재 시간 알려주는 API 추가해줘
> GET /api/random 하면 1-100 사이 랜덤 숫자 주는 API 추가해줘
```

### 🔴 도전: 파라미터 처리

```
> GET /api/greeting?name=홍길동 하면 "안녕하세요, 홍길동님!" 응답하게 해줘
```

---

## 🚨 흔한 에러와 해결법

### 에러 1: "command not found"

**화면에 이렇게 나옴:**
```
bash: node: command not found
```

**원인:** 해당 프로그램이 설치되지 않음

**해결:**
```
> node 설치해줘
```
또는 [nodejs.org](https://nodejs.org)에서 직접 설치

### 에러 2: "permission denied"

**화면에 이렇게 나옴:**
```
Error: EACCES: permission denied
```

**원인:** 권한 문제

**해결 (Mac/Linux):**
```
> sudo 붙여서 다시 실행해줘
```

**해결 (Windows):**
```
관리자 권한으로 PowerShell 실행
```

### 에러 3: "ENOENT: no such file or directory"

**화면에 이렇게 나옴:**
```
Error: ENOENT: no such file or directory
```

**원인:** 파일이나 폴더가 없음

**해결:**
```
> 현재 위치가 어디야?
> 이 폴더에 뭐가 있어?
```

### 에러 4: "port already in use"

**화면에 이렇게 나옴:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**원인:** 다른 프로그램이 같은 포트 사용 중

**해결:**
```
> 3000번 포트 사용 중인 프로세스 찾아서 종료해줘
```

또는 다른 포트 사용:
```
> 8080 포트로 서버 시작해줘
```

### 에러 5: "Cannot find module"

**화면에 이렇게 나옴:**
```
Error: Cannot find module 'express'
```

**원인:** 패키지가 설치되지 않음

**해결:**
```
> npm install 해줘
```

또는:
```
> express 설치해줘
```

### 에러 6: "npm ERR! code E404"

**화면에 이렇게 나옴:**
```
npm ERR! code E404
npm ERR! 404 Not Found
```

**원인:** 패키지 이름이 잘못됨

**해결:**
```
> 패키지 이름 확인해줘. react가 맞아?
```

### 에러 7: "EPERM: operation not permitted"

**화면에 이렇게 나옴:**
```
npm ERR! code EPERM
npm ERR! syscall unlink
```

**원인:** 파일이 다른 프로그램에서 사용 중

**해결:**
- VS Code 등 에디터 닫기
- node_modules 폴더 삭제 후 다시 설치:
```
> node_modules 삭제하고 다시 npm install 해줘
```

### 에러 8: "npm WARN deprecated"

**화면에 이렇게 나옴:**
```
npm WARN deprecated package@1.0.0: this package is deprecated
```

**의미:** 경고일 뿐, 에러는 아님

**해결:**
- 대부분 무시해도 됨
- 최신 대안이 있다면:
```
> 이 deprecated 패키지 대신 뭐 써야 해?
```

### 에러 9: "SyntaxError"

**화면에 이렇게 나옴:**
```
SyntaxError: Unexpected token '}'
```

**원인:** 코드 문법 오류

**해결:**
```
> 이 에러 고쳐줘. SyntaxError라고 나와.
```

### 에러 10: "npm run: command not found"

**화면에 이렇게 나옴:**
```
npm ERR! missing script: start
```

**원인:** package.json에 해당 스크립트가 없음

**해결:**
```
> package.json에 어떤 스크립트가 있어?
```

그리고 올바른 스크립트 이름 사용

---

## ❌ 자주 하는 실수

### 1. 잘못된 폴더에서 명령어 실행하기
```
> 지금 어디야?
```
로 항상 위치를 확인하세요.

> ⚠️ **주의사항**
>
> `npm install`은 package.json이 있는 폴더에서 실행해야 합니다.
> 다른 폴더에서 실행하면 새 package.json이 생성됩니다.

### 2. 명령어 끝날 때까지 안 기다리기
`npm install` 같은 명령어는 시간이 걸립니다. 완료 메시지가 나올 때까지 기다리세요.

```
진행 중:
[##########........] 50%

완료:
added 52 packages in 3s
```

### 3. 의존성 설치 잊어버리기
프로젝트를 처음 열면:
```
> npm install
```
을 먼저 실행해야 합니다.

> 💡 **비전공자 팁**
>
> 새 프로젝트를 받으면 무조건 `npm install` 먼저!
> 안 그러면 "module not found" 에러가 납니다.

### 4. 서버 실행 중에 터미널 닫기
서버는 터미널이 열려있어야 계속 동작합니다.

```
잘못된 방법:
서버 시작 → 터미널 닫기 → 서버 종료됨

올바른 방법:
서버 시작 → 터미널 열어두기 → 다른 터미널 탭에서 작업
```

### 5. 에러 메시지 무시하기
에러 메시지를 복사해서 Claude에게 보여주세요. 유용한 정보가 담겨있습니다.

```
> 이 에러 메시지 봐줘:
> Error: Cannot find module 'lodash'
```

### 6. sudo 남용하기
```
> sudo npm install  (권장하지 않음)
```
→ 권한 문제가 생기면 먼저 다른 해결책을 찾으세요.

### 7. node_modules 커밋하기
```
.gitignore에 node_modules 추가하세요!
```
→ 용량이 크고 npm install로 재생성 가능합니다.

---

## 🆘 안 되면?

| 증상 | 해결 방법 |
|------|----------|
| "command not found" | 프로그램 설치 필요 |
| "permission denied" | `sudo` 사용 또는 권한 확인 |
| "ENOENT" | 파일/폴더 경로 확인 |
| "port already in use" | 다른 프로세스 종료 또는 포트 변경 |
| 서버가 안 켜짐 | 에러 메시지 확인, 의존성 설치 확인 |
| 브라우저에서 안 보임 | 서버 실행 중인지 확인, Ctrl+F5로 새로고침 |
| npm install 실패 | 인터넷 연결 확인, node_modules 삭제 후 재시도 |

---

## 💡 현업에서는 이렇게 사용해요

### 프로젝트 시작

```bash
# 1. 저장소 클론
git clone https://github.com/example/project.git

# 2. 폴더 이동
cd project

# 3. 의존성 설치
npm install

# 4. 개발 서버 시작
npm run dev
```

### 일상적인 개발 흐름

```bash
# 아침: 최신 코드 받기
git pull

# 개발 서버 시작
npm run dev

# 새 기능 개발...

# 테스트
npm test

# 커밋
git add .
git commit -m "Add new feature"
git push
```

### 배포 전 체크

```bash
# 린트 검사
npm run lint

# 테스트
npm test

# 빌드
npm run build

# 빌드 결과 확인
npm run preview
```

---

## ✅ 체크리스트

학습을 마치기 전에 확인하세요:

- [ ] Claude에게 명령어 실행을 요청할 수 있다
- [ ] `!` 접두사의 용도를 안다
- [ ] npm install과 npm run의 차이를 안다
- [ ] package.json이 무엇인지 안다
- [ ] 간단한 서버를 만들고 실행할 수 있다
- [ ] 에러가 나면 Claude에게 도움을 요청할 수 있다
- [ ] localhost가 무엇인지 안다

---

## 🔑 핵심 용어

| 용어 | 설명 |
|------|------|
| **npm** | Node Package Manager. 패키지 관리 도구 |
| **패키지** | 재사용 가능한 코드 묶음 |
| **package.json** | 프로젝트 설정 파일 |
| **dependencies** | 실행에 필요한 패키지 목록 |
| **devDependencies** | 개발에만 필요한 패키지 목록 |
| **서버** | 요청을 받아 응답하는 프로그램 |
| **localhost** | 내 컴퓨터 자신을 가리키는 주소 |
| **포트** | 네트워크 통신의 문 번호 |

[전체 용어 사전](../GLOSSARY.ko.md)도 참고하세요.

---

## 📚 더 알아보기

**공식 문서:**
- [npm 공식 문서](https://docs.npmjs.com/) - npm 완벽 가이드
- [Node.js 공식 문서](https://nodejs.org/docs/latest/api/) - Node.js API 문서

**영상 자료:**
- [npm Tutorial for Beginners (YouTube)](https://www.youtube.com/results?search_query=npm+tutorial+beginners) - npm 기초 튜토리얼
- [Express.js Crash Course (YouTube)](https://www.youtube.com/results?search_query=express+js+crash+course) - Express 빠른 시작
- [Git Tutorial (YouTube)](https://www.youtube.com/results?search_query=git+tutorial+beginners) - Git 기초

**읽을거리:**
- [JavaScript.info](https://ko.javascript.info/) - 자바스크립트 튜토리얼 (한국어)
- [Express.js 공식 가이드](https://expressjs.com/ko/) - Express 시작하기

**유용한 도구:**
- [npm 패키지 검색](https://www.npmjs.com/) - npm 패키지 찾기
- [npx 가이드](https://www.npmjs.com/package/npx) - 패키지 직접 실행

---

## 🎉 Part 1 완료!

축하합니다! Part 1 (시작하기)을 완료했습니다.

**지금까지 배운 것:**
- AI 코딩과 바이브코딩의 개념
- Claude Code 설치 및 로그인
- 권한 모드와 기본 대화
- 파일 생성, 수정, 참조
- 터미널 명령어 실행
- npm 생태계와 서버 개념

**이제 할 수 있는 것:**
- Claude와 대화하며 파일 만들기
- 간단한 웹서버 실행하기
- 에러 해결 요청하기
- 패키지 설치하고 사용하기

다음 Part에서는 Claude Code의 핵심 기능들을 더 깊이 학습합니다!

---

## ➡️ 다음 단계

다음 챕터에서는 프로젝트 구조를 이해하고 분석하는 방법을 배웁니다.

**다음 챕터 미리보기:**
- 프로젝트 구조 분석하기
- 의존성 관계 이해하기
- 코드베이스 탐색 전략

[Chapter 06: 프로젝트 구조 이해](../Chapter06-Project-Structure/README.ko.md)로 진행하십시오.

---

<p align="center">
  <sub>Built with ❤️ by <a href="https://hashed.com">Hashed</a></sub>
</p>
